import {NextResponse} from "next/server";import {z} from "zod";import {getSession} from "@/lib/session";import {sellProduct} from "@/services/inventory";
const input=z.object({productId:z.coerce.number().int().positive(),quantity:z.coerce.number().int().positive(),unitSalePrice:z.coerce.number().int().positive(),paidAmount:z.coerce.number().int().min(0),customerName:z.string().min(1).max(160),customerPhone:z.string().regex(/^\+?\d{9,15}$/),paymentMethod:z.enum(["CASH","CARD","MIXED"])});
export async function POST(request:Request){const session=await getSession();if(!session)return NextResponse.json({error:"Ruxsat yo‘q"},{status:401});try{const data=input.parse(await request.json());const result=await sellProduct({...data,actor:{id:session.userId,role:session.role}});return NextResponse.json({ok:true,...result});}catch(error){console.error(error);const message=error instanceof z.ZodError?"Sotuv ma’lumotlarini tekshiring":error instanceof Error?error.message:"Sotuvda xato";return NextResponse.json({error:message},{status:400});}}
